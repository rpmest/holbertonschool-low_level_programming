#include <stdio.h>

/**
 * main - Prints all single digit numbers of baase 10 starting from 0, then new
 *line (this time using only putchar)
 *
 * Return: 0
 */
int main(void)
{
	int num;

	for (num = 49; num < 58; num++)
	{
		putchar();
	}
	/* Prints newline */
	putchar(10);

	return (0);
}
