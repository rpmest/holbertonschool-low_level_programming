#include <stdio.h>

/**
 * main - Prints all single digit numbers of baase 10 starting from 0, then new
 *line
 *
 * Return: 0
 */
int main(void)
{
	int num;

	for (num = 0; num < 10; num++)
	{
		printf("%i", num);
	}
	printf("\n");

	return (0);
}
